#include<stdio.h>
#include<string.h>
struct first
{char nm,fir[10],foll[10];
int ft,fw;
};stat[20];
char pro[20][20];int n,sptr=0;
void first(char x)
{


int main()
{int i,j;
int rpt[20];
printf("Enter the no of production");
scanf("%d",&n);
printf("Enter the production");
for(i=0;i<n;i++)
scanf("%s",pro[i]);
for(i=0;i<n;i++)
{for(k=0;k<(strlen(rpt);k++)
if(rpt[k]==pro[i][0]) break;
else
{rpt[k]=pro[i][0];
stat[sptr].ft=stat[spt].fw=0;
stat[sptr++].nm=pro[i][0];

}}
for(i=0;i<n;i++)
{first(pro[i][0]);
}
}
